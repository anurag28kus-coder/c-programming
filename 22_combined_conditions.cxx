#include <stdio.h>

int main (){
    int marks = 85 ; 
    int attendence = 80 ;
    
    if ( marks >= 40 && attendence >= 75){
        printf("Pass and Good Attendence ");
 
       }else {
           printf("Condition Not Met ");
       }
       return 0;
}